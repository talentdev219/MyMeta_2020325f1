using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class InputUpperCase : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }
    // Update is called once per frame
    void Update()
    {
        //GetComponent<Text>().text = char.ToUpper(GetComponent<Text>().text[0]) + GetComponent<Text>().text.Substring(1);
        //if (GetComponent<Text>().text.Length > 0)
        //{
        //    string[] words = GetComponent<Text>().text.Split(' ');
        //    GetComponent<Text>().text = "";
        //    for (int i = 0; i < words.Length; i++)
        //    {
        //        words[i] = char.ToUpper(words[i][0]) + words[i].Substring(1);
        //        GetComponent<Text>().text += words[i] + " ";
        //    }
        //}
    }
}
